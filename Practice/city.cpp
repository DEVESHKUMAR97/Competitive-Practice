#include<bits/stdc++.h>
#define ll long long int
#define mod 1000000007
using namespace std;
 

int main() {
  // your code goes here

  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  #ifndef ONLINE_JUDGE
  freopen("input.txt", "r", stdin);
  freopen("output.txt", "w", stdout);
  freopen("error.txt", "w", stderr);
  #endif

  
  ll sz, pos;
  while(cin >> sz >> pos, sz !=0 && pos != 0){
    ll mid, high = 1e5, low = 1;
    ll n = 0;
    while(low <= high){
      mid = low + (high - mid) / 2;
      if()
    }
  }
  return 0;
}  






